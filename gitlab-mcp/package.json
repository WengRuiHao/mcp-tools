{
  "name": "gitlab-mcp",
  "version": "0.3.0",
  "description": "Personal GitLab MCP server (Personal Access Token) — projects, branches, commits, file browsing, merge requests, issues, pipelines",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "gitlab-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
