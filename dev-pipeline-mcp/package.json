{
  "name": "dev-pipeline-mcp",
  "version": "0.1.0",
  "description": "Standalone MCP server that runs the Asana ticket pipeline (analyst -> engineer -> verifier -> tester). Exposes an MCP (stdio) interface for AI-driven callers and a small persistent HTTP bridge (dist/http-server.js) so PENDING_HUMAN_ACTIONS.html's checkboxes/buttons can resolve manual actions and record confirmations directly.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "dev-pipeline-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:http": "node dist/http-server.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
